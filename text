length = 50

for percent in (1..100).to_a do
	hashes = '#' * (percent * length / 100)
	spaces = ' ' * (length - hashes.length).to_i
	STDOUT.sync = true
	print "\rPercent: [#{hashes}#{spaces}](#{percent}/100)"
	sleep(1)
end
STDOUT.sync = false
puts 'Done!'